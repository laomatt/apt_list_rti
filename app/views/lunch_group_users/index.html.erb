<p id="notice"><%= notice %></p>

<h1>Lunch Group Users</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lunch_group_users.each do |lunch_group_user| %>
      <tr>
        <td><%= link_to 'Show', lunch_group_user %></td>
        <td><%= link_to 'Edit', edit_lunch_group_user_path(lunch_group_user) %></td>
        <td><%= link_to 'Destroy', lunch_group_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lunch Group User', new_lunch_group_user_path %>
