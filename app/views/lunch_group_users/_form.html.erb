<%= form_for(lunch_group_user) do |f| %>
  <% if lunch_group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lunch_group_user.errors.count, "error") %> prohibited this lunch_group_user from being saved:</h2>

      <ul>
      <% lunch_group_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
