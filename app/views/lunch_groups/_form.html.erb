<%= form_for(lunch_group) do |f| %>
  <% if lunch_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lunch_group.errors.count, "error") %> prohibited this lunch_group from being saved:</h2>

      <ul>
      <% lunch_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
